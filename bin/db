#!/usr/bin/env sh
# Host-side wrapper: forwards `bin/db <command>` to the backend container's
# bin/db script. Lets developers run database commands from the project root
# without typing `docker compose exec backend ...` every time.
set -e

exec docker compose exec backend bin/db "$@"
